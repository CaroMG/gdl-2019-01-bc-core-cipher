class Cipher {

encode(text) {
    let encodedText = text;
    return encodedText;
}

Decode(text) {
    let decodedText = text;
    return decodedText;
 }
}

let Cipher = new Cipher ();
console.log(cipher.encode("Hola Carolina"));
console.log(cipher.decode("Hola Carolina"));
